# CLAUDE.md

Use `AGENTS.md` as the project source of truth. It contains the repository map, command list, route rules, and launch verification expectations.

When working in subdirectories, also read the nearest nested `AGENTS.md`.
